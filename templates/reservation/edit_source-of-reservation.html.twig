{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}


{% block mainbtn %}
    <div class="header-elements d-none">
        <a href="{{ path('reservation_source-of-reservation_new') }}" class="btn btn-labeled btn-labeled-right bg-primary">{{ 'new'|trans([],'reservation') }} {{ 'source_of_reservation'|trans([],'reservation') }} <b><i class="icon-menu7"></i></b></a>
    </div>
{% endblock %}

{% block content %} 

    <div class="row">
        <div class="col-md-5">
            <!-- Basic layout-->
            {{ form_start(form) }}
            <div class="card border-teal-400">
                <div class="card-header bg-teal-400 text-white header-elements-inline">
                    <h5 class="card-title">{{ 'form'|trans([],'reservation') }}</h5>
                </div>

                <div class="card-body">
                    <div class="form-group row">
                        <label class="col-lg-3 col-form-label">{{ 'name'|trans([],'reservation') }}:</label>
                        <div class="col-lg-9">
                            {{ form_widget(form.name, {'attr':{'placeholder':"name" , 'class':"form-control"}}) }}
                        </div>
                    </div>

                    <div class="text-right">
                        <button type="submit" class="btn btn-primary">
                        {{ 'save'|trans([],'reservation') }}
                        <i class="icon-paperplane ml-2"></i>
                        </button>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}
            <!-- /basic layout -->
        </div>
    </div>

{% endblock %}


{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('global_assets/js/plugins/forms/styling/uniform.min.js') }}"></script>

    <script src="{{ asset('global_assets/js/demo_pages/form_layouts.js') }}"></script>
    
{% endblock %}
